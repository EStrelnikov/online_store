import React from "react";
import { Button } from "antd";

import classes from "./Main.module.scss";

const Main: React.FC = () => (
  <div className={classes.component}>Main
  </div>
);

export default Main;
